package ics.ci.authentification.repository;

import ics.ci.authentification.entity.Typegache;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TypegacheRepository extends JpaRepository<Typegache, Long> {
}

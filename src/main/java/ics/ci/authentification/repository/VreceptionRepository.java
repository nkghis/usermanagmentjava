package ics.ci.authentification.repository;

import ics.ci.authentification.entity.Vreception;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VreceptionRepository extends JpaRepository<Vreception, Long> {
}
